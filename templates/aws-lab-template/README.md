# Laboratory Template

This directory contains the reusable template used to create new AWS laboratories.

Each new laboratory should be created by copying the `lab-template` directory and replacing the placeholder content with the corresponding documentation, architecture diagrams and evidence.

The template ensures consistency across the entire AWS Cloud & DevOps Portfolio.